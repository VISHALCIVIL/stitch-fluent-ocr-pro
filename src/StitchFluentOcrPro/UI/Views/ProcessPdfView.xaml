<UserControl x:Class="StitchFluentOcrPro.UI.Views.ProcessPdfView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="650" d:DesignWidth="850">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="24">
            <!-- Header -->
            <TextBlock Text="Process PDFs" Style="{StaticResource DisplayLargeStyle}" Margin="0,0,0,8"/>
            <TextBlock Text="Select folders and execute offline Windows OCR batch processing." Style="{StaticResource BodyMediumStyle}" Margin="0,0,0,24"/>

            <!-- Folder Selector Card -->
            <Border Style="{StaticResource CardBorderStyle}">
                <StackPanel>
                    <TextBlock Text="Directory Configuration" Style="{StaticResource TitleMediumStyle}" Margin="0,0,0,16"/>

                    <!-- Input Directory Picker -->
                    <TextBlock Text="Main Input Folder (Recursive PDF Detection)" Style="{StaticResource BodyMediumStyle}" FontWeight="SemiBold" Margin="0,0,0,6"/>
                    <Grid Margin="0,0,0,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Grid.Column="0" Text="{Binding InputFolder, UpdateSourceTrigger=PropertyChanged}" Height="36" Padding="8,6" VerticalContentAlignment="Center"/>
                        <Button Grid.Column="1" Content="Browse..." Click="BrowseInputFolder_Click" Style="{StaticResource SecondaryButtonStyle}" Margin="8,0,0,0"/>
                    </Grid>

                    <!-- Output Directory Picker -->
                    <TextBlock Text="Output Folder (Preserves Subfolder Structure)" Style="{StaticResource BodyMediumStyle}" FontWeight="SemiBold" Margin="0,0,0,6"/>
                    <Grid Margin="0,0,0,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Grid.Column="0" Text="{Binding OutputFolder, UpdateSourceTrigger=PropertyChanged}" Height="36" Padding="8,6" VerticalContentAlignment="Center"/>
                        <Button Grid.Column="1" Content="Browse..." Click="BrowseOutputFolder_Click" Style="{StaticResource SecondaryButtonStyle}" Margin="8,0,0,0"/>
                    </Grid>

                    <!-- Control Action Bar -->
                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                        <Button Content="Scan Directory" Command="{Binding ScanFolderCommand}" Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,8,0"/>
                        <Button Content="Start OCR Batch" Command="{Binding StartCommand}" Style="{StaticResource PrimaryButtonStyle}" Margin="0,0,8,0"/>
                        <Button Content="Pause" Command="{Binding PauseCommand}" Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,8,0"/>
                        <Button Content="Resume" Command="{Binding ResumeCommand}" Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,8,0"/>
                        <Button Content="Stop Batch" Command="{Binding StopCommand}" Style="{StaticResource SecondaryButtonStyle}" Foreground="{StaticResource ErrorBrush}"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Execution Status & Live Progress Card -->
            <Border Style="{StaticResource CardBorderStyle}">
                <StackPanel>
                    <TextBlock Text="Execution Progress" Style="{StaticResource TitleMediumStyle}" Margin="0,0,0,12"/>
                    <ProgressBar Value="{Binding OverallProgress}" Maximum="100" Height="10" Margin="0,0,0,12" Foreground="{StaticResource PrimaryBrush}"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{Binding CurrentStats.CurrentFileName, StringFormat='Active: {0}'}" Style="{StaticResource MonoDataStyle}"/>
                        <TextBlock Grid.Column="1" Text="{Binding OverallProgress, StringFormat='{}{0:F1}% Complete'}" Style="{StaticResource BodyMediumStyle}" FontWeight="Bold"/>
                    </Grid>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
